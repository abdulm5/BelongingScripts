{
  "crons": [
    {
      "path": "/api/cron/prune-visits",
      "schedule": "0 3 * * *"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/[^.]+", "dest": "/", "status": 200 }
  ]
}
